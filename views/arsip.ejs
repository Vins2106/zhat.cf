<script>
        if (mehe == "me") {
          if (time) {
            name.textContent = `${user.Username}`
          } else {
            name.textContent = `${user.Username}`
          }
        } else {
          if (time) {
            name.textContent = `${user.Username}`
          } else {
            name.textContent = `${user.Username}`
          }
        }
        namemsg.appendChild(name)
</script>